# DuckMotion plugin-host dependencies
#
# DuckMotion's model runtimes are process-isolated. WebbDuck already supplies
# FastAPI/Pydantic/Pillow for the plugin host, so architecture-specific ML
# packages do not belong in this environment.
#
# Install model runtime environments separately from:
#   runtime_requirements/wan.txt
#   runtime_requirements/ltx25.txt
# and point DUCKMOTION_WAN_PYTHON / DUCKMOTION_LTX_PYTHON at those interpreters.
