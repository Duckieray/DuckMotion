# Wan isolated Diffusers runtime
#
# Install PyTorch separately for the target CUDA stack, then install this file
# in a dedicated environment and set DUCKMOTION_WAN_PYTHON to that Python.
# Diffusers 0.39.0 contains the current WanPipeline/WanImageToVideoPipeline APIs.
diffusers==0.39.0
transformers>=4.57.0,<6
accelerate>=1.12.0,<2
safetensors>=0.5.0
huggingface_hub>=0.36.0
sentencepiece>=0.2.0
pillow>=11.0.0
numpy>=2.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.1
