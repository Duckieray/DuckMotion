# LTX-2.5 two-stage isolated Diffusers runtime
#
# Install PyTorch separately for the target CUDA stack, then install this file
# in a dedicated environment and set DUCKMOTION_LTX_PYTHON to that Python.
# LTX-2.5 APIs are not yet in stable Diffusers, so pin the exact Diffusers-main
# commit used for the first WebbDuck/DuckMotion hardware validation baseline.
git+https://github.com/huggingface/diffusers.git@4c52cb267ab60d239e3afccaa54ff7bcaa7f25fb
transformers>=4.57.0
accelerate>=1.12.0,<2
peft>=0.13.1
safetensors>=0.5.0
huggingface_hub>=0.36.0
sentencepiece>=0.2.0
pillow>=11.0.0
numpy>=2.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.1
