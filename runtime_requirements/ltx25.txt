# LTX-2.5 two-stage Diffusers runtime
#
# LTX-2.5 support currently lives on Diffusers main rather than a stable
# release. Install this in a dedicated environment and set DUCKMOTION_LTX_PYTHON
# to its Python executable. Install a matching PyTorch build separately for the
# host CUDA stack. This environment must provide the LTX latent upsampler and
# stage-2 distilled sigma APIs used by DuckMotion's production path.
git+https://github.com/huggingface/diffusers.git
transformers>=4.57.0
accelerate>=1.0.0
safetensors>=0.5.0
huggingface_hub>=0.36.0
sentencepiece>=0.2.0
pillow>=11.0.0
numpy>=2.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.1
