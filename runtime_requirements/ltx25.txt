# LTX-2.5 Diffusers runtime
#
# LTX-2.5 support currently lives on Diffusers main rather than a release.
# Install this in a dedicated environment and set DUCKMOTION_LTX_PYTHON to its
# Python executable. Install a matching torch build separately for CUDA/ROCm.
git+https://github.com/huggingface/diffusers.git
transformers>=4.57.0
accelerate>=1.0.0
safetensors>=0.5.0
huggingface_hub>=0.36.0
pillow>=11.0.0
numpy>=2.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.1
